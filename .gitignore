# Node
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
package-lock.json
yarn.lock
pnpm-lock.yaml

# Environment
.env
.env.local
.env.*.local

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
*.swp
*.swo

# Claude Code (personal dev config — never committed)
# Skills/commands/agents are distributed as plugins in this repo.
# A contributor's local .claude/ is their own setup, not project state.
.claude/

# CLAUDE.md is the maintainer's local working instructions, not project truth.
# Removed from git history on 2026-07-28. Contributor-facing rules that used to
# live here (notably the plugin version-bump requirement) are in CONTRIBUTING.md.
CLAUDE.md

# Build / output
dist/
build/
out/
output/
coverage/

# Plugin runtime data
data/
*.log

# Superpowers SDD scratch (plan ledgers, briefs, review packages) — never published
.superpowers/

# Playwrite scratch (playwright test artifacts) — never published
.playwright-mcp/

# flow plugin working state — private to this machine, never published.
# Covers the runtime arming markers (.flow/.allow-*), machine-local overrides
# (.flow/local.md), and session state (.flow/session-progress.md, session-log.md).
# Note: flow's own docs treat .flow/config.md as shareable if a project wants it.
# This repo does not — flow here is the author's personal setup, not project truth.
.flow/
